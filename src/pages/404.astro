---
import Layout from "../layouts/Layout.astro";
---

<Layout title="404 | 找不到頁面">
  <main id="page-404" class="wrap">
    <!-- 全頁背景在 CSS 處理，這裡只放內容 -->
    <section class="card" role="article" aria-labelledby="err-title">
      <div class="card-inner">
        <div class="left">
          <!-- 精簡後的裝飾：保留簡單 ring 作為視覺焦點（非動畫）-->
          <svg class="decor-ring" viewBox="0 0 120 120" width="84" height="84" aria-hidden="true">
            <defs>
              <linearGradient id="gA" x1="0%" x2="100%"><stop offset="0%" stop-color="#7dd3fc"/><stop offset="100%" stop-color="#3b82f6"/></linearGradient>
            </defs>
            <circle cx="60" cy="60" r="40" stroke="#e6eefb12" stroke-width="8" fill="none"/>
            <circle cx="60" cy="60" r="32" stroke="url(#gA)" stroke-width="6" fill="none" stroke-linecap="round" />
          </svg>
        </div>

        <div class="right">
          <h1 id="err-title" class="code">404</h1>
          <p class="subtitle">你走得有點遠了。</p>
          <p class="desc">
            這個頁面不存在，可能被刪掉、改名，或只是從來沒存在過。
          </p>

          <div class="actions">
            <a href="/" class="btn back-home" aria-label="回首頁">← 回首頁</a>
          </div>

          <p class="hint">提示：檢查網址是否正確，或回到首頁瀏覽其他內容。</p>
        </div>
      </div>
    </section>
  </main>
</Layout>

<style is:inline>
/* ---------- 變數 & 基本 reset ---------- */
:root{
  --bg-top: #071023;
  --bg-bottom: #071726;
  --card-bg: rgba(255,255,255,0.02);
  --card-border: rgba(255,255,255,0.04);
  --glass-shadow: rgba(2,6,23,0.45);
  --primary-1: #60a5fa;
  --primary-2: #2563eb;
  --text: #e6eefb;
  --muted: rgba(230,238,251,0.66);
  --max-width: 920px;
  --gap: 1rem;
  --radius: 14px;
}

/* make background cover entire viewport (html/body) */
html,body { height: 100%; margin: 0; padding: 0; }

/* wrap covers full viewport so background always填滿 */
.wrap {
  min-height: 100vh;
  display: flex;
  align-items: center;
  justify-content: center;
  padding: clamp(1rem, 3vw, 2.2rem);
  position: relative;
  box-sizing: border-box;
  background: linear-gradient(180deg, var(--bg-top) 0%, var(--bg-bottom) 100%);
  background-attachment: fixed;
  color: var(--text);
  font-family: Inter, ui-sans-serif, system-ui, -apple-system, "Segoe UI", Roboto, "Helvetica Neue", Arial;
  -webkit-font-smoothing: antialiased;
  -moz-osx-font-smoothing: grayscale;
}

/* subtle full-page vignette to add depth without animations */
.wrap::after {
  content: "";
  position: absolute;
  inset: 0;
  background: radial-gradient(1200px 600px at 10% 10%, rgba(59,130,246,0.06), transparent 10%),
              radial-gradient(900px 400px at 90% 80%, rgba(124,58,237,0.04), transparent 10%);
  pointer-events: none;
  z-index: 0;
}

/* ---------- Card ---------- */
.card {
  position: relative;
  z-index: 5;
  width: min(96%, var(--max-width));
  border-radius: var(--radius);
  background: var(--card-bg);
  border: 1px solid var(--card-border);
  box-shadow: 0 12px 36px var(--glass-shadow);
  padding: clamp(1.25rem, 2.5vw, 2.2rem);
  overflow: visible;
}

/* inner layout: left decor + right content */
.card-inner {
  display: grid;
  grid-template-columns: 92px 1fr;
  gap: clamp(0.8rem, 2vw, 1.2rem);
  align-items: center;
}

/* left decorative column */
.left { display:flex; align-items:center; justify-content:center; padding-left: 4px; }
.decor-ring { display:block; }

/* right content column */
.right { padding: 0; }

/* 404 字樣 */
.code {
  font-size: clamp(2.8rem, 7.5vw, 4.6rem);
  margin: 0 0 6px 0;
  line-height: 1;
  font-weight: 900;
  background: linear-gradient(135deg, #a5b4fc, #60a5fa 60%, #2563eb);
  -webkit-background-clip: text;
  -webkit-text-fill-color: transparent;
}

/* subtitle & desc */
.subtitle {
  margin: 0 0 6px 0;
  font-size: 1.02rem;
  color: var(--text);
  font-weight: 600;
}
.desc {
  margin: 0 0 12px 0;
  color: var(--muted);
  font-size: 0.98rem;
  line-height: 1.6;
}

/* actions: only one main CTA */
.actions { margin-top: 8px; display:flex; gap: .6rem; align-items:center; }
.btn {
  display:inline-flex; align-items:center; gap:.5rem; justify-content:center;
  padding:.56rem 1rem; border-radius:999px; font-weight:700; cursor:pointer;
  text-decoration:none; color: white; border: none;
  transition: transform .12s ease, box-shadow .12s ease, opacity .12s ease;
}
.back-home {
  background: linear-gradient(90deg,var(--primary-1), var(--primary-2));
  box-shadow: 0 8px 22px rgba(37,99,235,0.12);
}
.back-home:hover { transform: translateY(-3px); box-shadow: 0 18px 40px rgba(37,99,235,0.14); }

/* hint */
.hint {
  margin-top: 12px;
  color: var(--muted);
  font-size: 0.92rem;
}

/* responsive: stack on narrow screens */
@media (max-width: 640px) {
  .card-inner { grid-template-columns: 72px 1fr; gap: .7rem; }
  .decor-ring { width: 64px; height: 64px; }
  .code { font-size: clamp(2.4rem, 12vw, 3.6rem); }
  .card { padding: 1.2rem; border-radius: 12px; }
}

/* accessibility: respect reduced motion */
@media (prefers-reduced-motion: reduce) {
  * { animation-duration: 0s !important; animation-iteration-count: 1 !important; transition-duration: 0s !important; }
}

/* misc small polish */
.back-home:focus { outline: 3px solid rgba(96,165,250,0.14); outline-offset: 3px; }
</style>
